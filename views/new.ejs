<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New chats</title>
    <style>
        form {
            width: 80%;
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 2px 8px rgba(0, 0, 0, 0.12);
        }

        input,
        textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            font-size: 15px;
            border: 1px solid #ccc;
            border-radius: 6px;
            outline: none;
            transition: 0.2s;
        }

        /* Focus effect */
        input:focus,
        textarea:focus {
            border-color: #00a8cc;
            box-shadow: 0px 0px 6px rgba(0, 168, 204, 0.4);
        }

        /* Textarea size */
        textarea {
            height: 100px;
            resize: none;
        }

        /* Button (same as previous style) */
        button {
            background-color: #00a8cc;
            color: white;
            padding: 10px 18px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 15px;
            margin-top: 10px;
            transition: 0.2s ease-in-out;
            display: block;
            margin-left: auto;
            margin-right: auto;
        }

        button:hover {
            background-color: #0089a8;
            box-shadow: 2px 2px 8px rgba(16, 132, 158, 0.541);
            transform: scale(1.04);
        }
    </style>
</head>

<body>
    <h3>Write new chat..</h3>
    <form method="POST" action="/chats">
        <input placeholder="who are you?" name="from" />
        <textarea name="msg" placeholder="Write your message here.."></textarea>
        <input placeholder="add receiver" name="to" />
        <button>Create Chat</button>
    </form>
</body>

</html>